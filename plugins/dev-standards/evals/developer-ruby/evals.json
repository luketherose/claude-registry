[
  {
    "agent": "developer-ruby",
    "query": "Rifattorizza questo fat ActiveRecord model estraendo dei service object",
    "files": [],
    "expected_behavior": [
      "Business logic moves into service objects and the controller action shrinks to dispatch plus response",
      "Every produced Ruby file starts with `# frozen_string_literal: true`",
      "Queries are expressed as scopes or query objects: no raw .where survives inside a controller",
      "Loads `testing-standards` with the Skill tool (tool_used: Skill), and the effect is visible: each extracted service object ships with an RSpec spec whose describe / context / it nesting covers happy path, boundary, invalid input and error propagation as separate examples"
    ]
  },
  {
    "agent": "developer-ruby",
    "query": "Scrivi un worker Sidekiq idempotente per l'invio delle notifiche",
    "files": [],
    "expected_behavior": [
      "The job receives an identifier, not an ActiveRecord instance",
      "Idempotency is enforced explicitly (a guard, a lock or a status check) rather than assumed from retry behaviour",
      "No `rescue Exception` appears anywhere in the produced code"
    ]
  },
  {
    "agent": "developer-ruby",
    "query": "Review this Rails 7 controller for RuboCop and RSpec coverage",
    "files": [
      "fixtures/orders_controller.rb"
    ],
    "expected_behavior": [
      "Reports violations by cop name with the fixtures/orders_controller.rb line: Style/FrozenStringLiteralComment for the missing magic comment, Rails/WhereEquals for the string conditions in index, Rails/FindBy for the where(...).first in load_order, and Lint/RescueException for the rescue in update",
      "Flags the three before_action callbacks that run business logic (apply_discount, recalculate_totals and notify_warehouse each save or call out during a filter) and the reopened core String class adding to_money as monkey patching",
      "Proposes RSpec specs for the uncovered paths rather than Minitest"
    ]
  }
]
