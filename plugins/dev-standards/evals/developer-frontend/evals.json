[
  {
    "agent": "developer-frontend",
    "query": "Migra questa UI Streamlit in Angular",
    "files": [],
    "expected_behavior": [
      "Names Angular as the detected target before writing code, and the deliverable contains no React or Vue idiom: no useState, no useEffect, no JSX and no <template> block",
      "Delivers the Angular component as four files (.ts, .html, .scss, .spec.ts) with no inline template: or styles: literal in the .ts",
      "Handles loading, empty and error states explicitly in the component",
      "Produces a standalone component declared with ChangeDetectionStrategy.OnPush and SCSS that references design tokens instead of literal colour values, the `angular-expert` and `css-expert` conventions, and no React, Vue or Qwik idiom appears anywhere in the output"
    ]
  },
  {
    "agent": "developer-frontend",
    "query": "Review this React hook for correctness",
    "files": [
      "fixtures/useOrders.ts"
    ],
    "expected_behavior": [
      "Flags each of these with its fixtures/useOrders.ts line: the `any` on the props parameter, the `any` on the useState generic, and the `any` on the store selector callback",
      "Reports that orderFilter is held in globalStore although it is read only inside this hook, and says it belongs in local component state",
      "Reports the hardcoded values in the style object (#1f2a44, #FFFFFF, 13px, 15px) as design token violations, naming each one"
    ]
  },
  {
    "agent": "developer-frontend",
    "query": "Scrivi il componente Vue 3 per la lista ordini con loading, empty ed errore",
    "files": [],
    "expected_behavior": [
      "Delivers a Vue SFC plus its spec file, not a single untested file",
      "Interactive elements are keyboard navigable and carry ARIA roles",
      "Includes a test plan naming the scenarios covered for the produced components"
    ]
  }
]
