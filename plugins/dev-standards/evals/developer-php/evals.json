[
  {
    "agent": "developer-php",
    "query": "Review this PHP code for PHPStan level 8 violations",
    "files": [
      "fixtures/OrderService.php"
    ],
    "expected_behavior": [
      "Flags the untyped $payload associative array used as a DTO and every missing type in fixtures/OrderService.php (the $repository and $logger properties, both constructor parameters, and the place() parameter and return type), each with its line",
      "Flags, each with its line, the two @ suppressions on the $payload lookups, the catch (\\Throwable) in place() that returns null, the __call and __get magic methods, and the eval() of $payload['pricing_rule']",
      "Names what must change for fixtures/OrderService.php to pass PHPStan level 8 and PSR-12: declare(strict_types=1), a typed DTO or readonly class replacing the array, property and parameter types, and removal of eval() and the magic methods"
    ]
  },
  {
    "agent": "developer-php",
    "query": "Rifattorizza questo fat Eloquent model in un service layer",
    "files": [],
    "expected_behavior": [
      "Moves the business logic out of the model into service classes, leaving the model responsible for persistence only",
      "Every produced PHP file opens with declare(strict_types=1);",
      "Eloquent or Doctrine entities do not cross the queue boundary: jobs receive identifiers or DTOs"
    ]
  },
  {
    "agent": "developer-php",
    "query": "Scrivi un command Symfony 7 che importa gli ordini, con enum e readonly class",
    "files": [],
    "expected_behavior": [
      "Closed sets are modelled as enums and value objects use readonly classes with typed properties",
      "No Eloquent or Doctrine query call appears inside the Symfony command class: every read and write goes through an injected repository",
      "Produces Pest or PHPUnit 10 tests alongside the command",
      "Each produced test is named {method}_{condition}_{outcome} and its body is laid out Arrange, Act, Assert, the naming and structure `testing-standards` prescribes"
    ]
  }
]
