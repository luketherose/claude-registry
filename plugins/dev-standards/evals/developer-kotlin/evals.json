[
  {
    "agent": "developer-kotlin",
    "query": "Review this Kotlin code for null-safety issues",
    "files": [
      "fixtures/OrderService.kt"
    ],
    "expected_behavior": [
      "Flags every `!!` in fixtures/OrderService.kt (customerId!!, total!!, findCustomer(id)!! and customer.email!!) and both lateinit var fields of OrderService, each with its line, and proposes constructor injection instead",
      "Flags the Java style carried into Kotlin in fixtures/OrderService.kt: the getLastOrder()/setLastOrder() accessor pair, the Optional<OrderEntity> return type, and the @Throws checked exception declaration",
      "Flags OrderEntity, declared as a data class and annotated @Entity, as a data class used for a JPA entity, and states the identity consequence of the generated equals/hashCode and copy()"
    ]
  },
  {
    "agent": "developer-kotlin",
    "query": "Converti questo service Java in Kotlin idiomatico",
    "files": [],
    "expected_behavior": [
      "DTOs become data classes and closed state sets become sealed classes, with invariants encoded in value classes",
      "val is the default and var appears only where mutation is actually required, with explicit visibility on the public API",
      "Delivers the file through the Write or Edit tool rather than a Bash heredoc",
      "The converted Kotlin service takes its collaborators through the constructor and returns a DTO rather than the entity, the bean shape `java-spring-standards` and `spring-architecture` prescribe"
    ]
  },
  {
    "agent": "developer-kotlin",
    "query": "Scrivi un endpoint Ktor con coroutine e structured concurrency",
    "files": [],
    "expected_behavior": [
      "Uses coroutines with structured concurrency and contains no Thread.sleep inside coroutine code",
      "No apply, run, with or also block in the produced endpoint spans more than three statements, and no such block both mutates its receiver and returns a different value",
      "The produced code satisfies the ktlint and detekt rules it cites: explicit visibility on every public declaration, no wildcard import, no line longer than 120 characters, and no function longer than 30 lines"
    ]
  }
]
